# Design Phase - ระบบที่ง่ายที่สุด สำหรับ 5 ชั่วโมง

## กลยุทธ์การให้คะแนนสูงสุด (80 คะแนนเต็ม)

### เป้าหมาย: ได้คะแนนสูงสุดด้วยความพยายามน้อยที่สุด

---

## 📊 การวิเคราะห์คะแนนและลำดับความสำคัญ

### ส่วนที่ 2: ตั้งค่าเครือข่าย (20 คะแนน) - **ทำทั้งหมด**
**เวลา: ~1.5 ชั่วโมง**

#### 2.1 Virtualization (3 คะแนน) - ⭐ ง่ายมาก
- ใช้ **Proxmox** (ตามเงื่อนไข)
- สร้าง Production VM บน Proxmox
- ตั้งค่า Network: Bridge Adapter เชื่อมต่อกับ Router

#### 2.2 Linux Server (4 คะแนน) - ⭐ ง่าย
- ใช้ **Ubuntu Server 22.04 LTS** (ISO) บน Production VM
- ตั้งค่า Static IP: 192.168.1.20 (Production VM)
- ตั้ง hostname: `prod-server`
- สร้าง user: `it` (ไม่ใช่ root)
- สร้าง directory: `/home/it/project/` สำหรับเก็บโปรเจคทั้งหมด
- เปิด SSH, ปิด services ที่ไม่จำเป็น

#### 2.3 SSH Remote (3 คะแนน) - ⭐ ง่ายมาก
- เปิด SSH service
- สร้าง user `it` (ไม่ใช่ root)
- ตั้งค่า UFW: เปิด port 22
- ตั้งค่า VS Code Remote SSH: เชื่อมต่อไปที่ Production VM
- เปิดโปรเจคใน `/home/it/project/` จาก VS Code (เครื่อง DEV)

#### 2.4 DevOps Tools (3 คะแนน) - ⭐ ง่าย (ใช้ Docker)
- ติดตั้ง Docker: `curl -fsSL https://get.docker.com | sh`
- ติดตั้ง GitLab: ใช้ Docker Compose (ง่ายที่สุด)
- ติดตั้ง GitLab Runner: ใช้ Docker

#### 2.5 Dockerfile/docker-compose (2 คะแนน) - ⭐ ง่ายมาก
- สร้าง Dockerfile สำหรับ Node.js app ใน `/home/it/project/`
- สร้าง docker-compose.yml: app + MySQL + MQTT + Node-RED
- เก็บไฟล์ทั้งหมดใน `/home/it/project/`

#### 2.6 CI/CD Pipeline (2 คะแนน) - ⭐ ง่าย
- สร้าง `.gitlab-ci.yml` แบบง่าย
- กำหนด stages: build, test, deploy

#### 2.7 Test Pipeline (3 คะแนน) - ⭐ ง่าย
- Push code ไป GitLab
- ดู Pipeline ทำงาน

---

### ส่วนที่ 3: MQTT + Node-RED (10 คะแนน) - **ทำแบบง่าย**
**เวลา: ~1 ชั่วโมง**

#### 3.1 MQTT Server + Node-RED (1 คะแนน) - ⭐ ง่ายมาก
- ใช้ Docker: `eclipse-mosquitto` + `nodered/node-red`
- ตั้งค่า ACL แบบง่าย (username/password)

#### 3.2 Test MQTT Client (1 คะแนน) - ⭐ ง่ายมาก
- ใช้ MQTT Explorer (GUI) หรือ Node-RED MQTT nodes
- Pub/Sub ทดสอบ

#### 3.3 Hardware Integration (1 คะแนน) - ⭐ ต้องทำ
- ใช้ **ESP32 จริง** + Sensor
- เขียนโค้ด Arduino สำหรับ ESP32
- ESP32 Pub ค่า sensor ไปยัง MQTT Topic
- ESP32 Sub ค่าเพื่อรับคำสั่งจาก Server
- เชื่อมต่อ ESP32 กับ Router TP-LINK (WiFi: @iot)

#### 3.4 Database Logging (1 คะแนน) - ⭐ ง่าย
- ใช้ Node-RED flow: MQTT In → Function → MySQL Out
- บันทึกข้อมูลลง MySQL

#### 3.5 Dashboard (6 คะแนน) - ⭐ ต้องทำ
- Node-RED Dashboard: แสดงข้อมูลเรียลไทม์ (1 คะแนน)
- Web Application Dashboard: 5 รูปแบบ (5 คะแนน) ⚠️ **สำคัญมาก**
  - Line Chart (กราฟเส้น)
  - Bar Chart (กราฟแท่ง)
  - Pie Chart (กราฟวงกลม)
  - Table (ตาราง)
  - Gauge/Card (เกจ/การ์ด)

---

### ส่วนที่ 4: ซอฟต์แวร์ (50 คะแนน) - **ทำแบบง่ายที่สุด**
**เวลา: ~2.5 ชั่วโมง**

#### 4.1 Flow Diagram (5 คะแนน) - ⭐ ง่ายมาก (ใช้ Draw.io)
- Login Flow
- Admin Flow
- User Level 1 Flow
- User Level 2 Flow
- System Overview

#### 4.2 Database Design (5 คะแนน) - ⭐ ง่าย
- สร้าง 3 ตาราง: `users`, `sensor_data`, `notifications`
- ER Diagram (Draw.io)
- ใช้ Data Type ที่เหมาะสม

#### 4.3 Screen Design (5 คะแนน) - ⭐ ง่าย (ใช้ Draw.io/Wireframe)
- Template Layout: Admin, User1, User2, Dashboard, Login
- วาดแบบง่ายๆ ไม่ต้องสวยมาก

#### 4.4 RESTful API Design (5 คะแนน) - ⭐ ง่าย (เขียนเอกสาร)
- ออกแบบ API: GET, POST, PUT, DELETE
- ตัวอย่าง Response, Error Handling
- JWT Security

#### 4.5 Back-end API (10 คะแนน) - ⭐ ต้องทำ (สำคัญ)
**ทำแบบง่ายที่สุด:**
- GET API: `/api/sensors` (query params)
- POST API: `/api/sensors` (body)
- POST API: `/api/upload` (file upload)
- POST API: `/api/login` (JWT)
- Error Handling
- Response Format: `{status, message, data}`
- API Documentation (เขียนใน README.md)
- Structure: routes/, controllers/, middleware/
- JWT Authentication

#### 4.6 Front-end (10 คะแนน) - ⚠️ **ทำแบบง่ายที่สุด**
**ทำแค่ที่จำเป็น:**
- Layout + Responsive (ใช้ Tailwind CSS)
- Login Page (JWT)
- Role-based Access (Admin, User1, User2)
- Dashboard: 5 รูปแบบการแสดงผล (Line, Bar, Pie, Table, Gauge)
- CRUD: เพิ่มข้อมูล (form)
- File Upload
- Edit/Delete (แจ้งเตือน)
- Search/Filter
- Pagination
- Validation (Frontend)
- Error Handling
- Dockerfile (Backend + Frontend)
- docker-compose.yml (app + db)
- Pipeline: build, test, deploy
- Auto trigger on push

---

## 🎯 สรุป: สิ่งที่ต้องทำ (5 ชั่วโมง)

### ชั่วโมงที่ 1: Network Setup (1.5 ชม.)
1. ติดตั้ง Proxmox
2. สร้าง Production VM (Ubuntu Server)
3. ตั้งค่า Router TP-LINK: SSID `@iot`, Password `12345678`
4. ตั้งค่า Network, Static IP, SSH
5. สร้าง user `it` และ directory `/home/it/project/`
6. ตั้งค่า VS Code Remote SSH
7. ติดตั้ง Docker

### ชั่วโมงที่ 2: DevOps + MQTT + Hardware (1 ชม.)
1. เขียน Dockerfile + docker-compose.yml ใน `/home/it/project/`
2. ติดตั้ง MQTT + Node-RED (Docker)
3. เขียนโค้ด ESP32 (Arduino) เชื่อมต่อ WiFi + MQTT
4. ทดสอบ ESP32 ส่งข้อมูลไป MQTT
5. ตั้งค่า GitLab CI/CD Pipeline (ถ้ามี GitLab)

### ชั่วโมงที่ 3: Database + Backend API (1.5 ชม.)
1. ออกแบบ Database (3 ตาราง)
2. สร้าง Backend API (Express.js)
   - GET, POST, Login (JWT), File Upload
   - Error Handling
   - MySQL Connection
3. ทดสอบ API (Thunder Client)

### ชั่วโมงที่ 4: Frontend + Dashboard (1.5 ชม.)
1. สร้าง Frontend (Vue 3 + Nuxt + Tailwind)
2. Login Page + JWT
3. Dashboard: 5 รูปแบบ (Chart.js หรือ Vue Chart)
4. CRUD: เพิ่ม/แก้ไข/ลบ
5. Search, Filter, Pagination

### ชั่วโมงที่ 5: Integration + Testing (0.5 ชม.)
1. เชื่อมต่อ Frontend-Backend
2. เชื่อมต่อ MQTT → Database → Dashboard
3. ทดสอบ Pipeline
4. ทดสอบระบบทั้งหมด

---

## 📋 สิ่งที่ **ไม่ต้องทำ** (ประหยัดเวลา)

1. ❌ Mobile Application - ทำแค่ Web App
2. ❌ Complex Features - ทำแค่พื้นฐานที่ได้คะแนน
3. ❌ Beautiful UI - ทำแค่ใช้งานได้ + Responsive
4. ❌ Complex Security - ทำแค่ JWT พื้นฐาน
5. ❌ GitLab VM แยก - ใช้ GitLab ใน Docker หรือใช้ GitLab.com

---

## 🛠️ Tech Stack ที่เลือก (ง่ายที่สุด)

### Backend
- **Node.js 18+** + **Express**
- **MySQL** (ง่ายกว่า MongoDB)
- **JWT** (jsonwebtoken)
- **Multer** (file upload)

### Frontend
- **Vue 3** + **Nuxt 3**
- **Tailwind CSS**
- **Chart.js** หรือ **Vue-Chartjs** (Dashboard)
- **Axios** (API calls)

### DevOps
- **Docker** + **Docker Compose**
- **GitLab** (Docker)
- **GitLab CI/CD**

### MQTT
- **eclipse-mosquitto** (Docker)
- **Node-RED** (Docker)
- **MQTT Explorer** (Desktop App)

---

## 📁 โครงสร้างโปรเจค (ใน Production VM)

**Path: `/home/it/project/`**

```
/home/it/project/
├── backend/
│   ├── src/
│   │   ├── routes/
│   │   │   ├── auth.js
│   │   │   ├── sensors.js
│   │   │   └── upload.js
│   │   ├── controllers/
│   │   ├── middleware/
│   │   │   └── auth.js
│   │   ├── models/
│   │   └── app.js
│   ├── Dockerfile
│   └── package.json
├── frontend/
│   ├── pages/
│   │   ├── login.vue
│   │   ├── dashboard.vue
│   │   └── sensors.vue
│   ├── components/
│   │   └── charts/
│   ├── Dockerfile
│   └── nuxt.config.js
├── esp32/
│   └── esp32_mqtt.ino (โค้ด Arduino สำหรับ ESP32)
├── docker-compose.yml
├── .gitlab-ci.yml
├── docs/
│   ├── flow-diagrams/ (Draw.io files)
│   ├── er-diagram.drawio
│   └── api-documentation.md
└── README.md
```

**หมายเหตุ:** 
- ทำงานทั้งหมดใน Production VM
- ใช้ VS Code Remote SSH เพื่อแก้ไขโค้ดจากเครื่อง DEV
- ESP32 เชื่อมต่อกับ Router TP-LINK (SSID: @iot)

---

## 🎯 เป้าหมายคะแนน

| ส่วน | คะแนนเต็ม | คะแนนที่คาดหวัง | ความยาก |
|------|----------|----------------|---------|
| ส่วนที่ 2: Network | 20 | 20 | ⭐ ง่าย |
| ส่วนที่ 3: MQTT | 10 | 10 | ⭐⭐ ปานกลาง |
| ส่วนที่ 4: Software | 50 | 40-45 | ⭐⭐⭐ ยาก |
| **รวม** | **80** | **70-75** | - |

---

## ⚡ Quick Start Checklist

### Phase 1: Setup (1.5 ชม.)
- [ ] ติดตั้ง Proxmox
- [ ] สร้าง Production VM (Ubuntu Server 22.04)
- [ ] ตั้งค่า Router TP-LINK: SSID `@iot`, Password `12345678`
- [ ] ตั้งค่า Network (Static IP: 192.168.1.20)
- [ ] ตั้งค่า SSH
- [ ] สร้าง user `it` และ directory `/home/it/project/`
- [ ] ตั้งค่า VS Code Remote SSH
- [ ] ติดตั้ง Docker

### Phase 2: DevOps (0.5 ชม.)
- [ ] เขียน Dockerfile (Backend)
- [ ] เขียน Dockerfile (Frontend)
- [ ] เขียน docker-compose.yml
- [ ] เขียน .gitlab-ci.yml
- [ ] ทดสอบ Pipeline

### Phase 3: MQTT + Hardware (0.5 ชม.)
- [ ] ติดตั้ง Mosquitto (Docker) ใน `/home/it/project/`
- [ ] ติดตั้ง Node-RED (Docker)
- [ ] เขียนโค้ด ESP32 (Arduino): เชื่อมต่อ WiFi (@iot) + MQTT
- [ ] ทดสอบ ESP32 Pub/Sub กับ MQTT Broker
- [ ] สร้าง Node-RED Flow (Database Logging)
- [ ] สร้าง Node-RED Dashboard

### Phase 4: Backend (1.5 ชม.)
- [ ] สร้าง Database (3 ตาราง)
- [ ] สร้าง API: GET, POST, Login, Upload
- [ ] JWT Authentication
- [ ] Error Handling
- [ ] API Documentation

### Phase 5: Frontend (1.5 ชม.)
- [ ] สร้าง Login Page
- [ ] สร้าง Dashboard (5 รูปแบบ)
- [ ] CRUD Operations
- [ ] Search, Filter, Pagination
- [ ] Validation + Error Handling

### Phase 6: Integration (0.5 ชม.)
- [ ] เชื่อมต่อ Frontend-Backend
- [ ] เชื่อมต่อ MQTT → Database → Dashboard
- [ ] ทดสอบระบบทั้งหมด

---

## 💡 Tips สำหรับมือใหม่

1. **ใช้ Docker Compose** - ง่ายที่สุด ไม่ต้องตั้งค่าซับซ้อน
2. **ใช้ Template/Boilerplate** - หา Node.js + Vue 3 template จาก GitHub
3. **ใช้ Library สำเร็จรูป** - Chart.js, Tailwind UI Components
4. **ทำทีละส่วน** - อย่าทำหลายอย่างพร้อมกัน
5. **ทดสอบบ่อยๆ** - ทดสอบทุกขั้นตอน
6. **บันทึกทุกอย่าง** - จดบันทึก command, config ที่ใช้

---

## 📚 Resources ที่ต้องเตรียม

1. **Proxmox** (ติดตั้งบนเครื่องแม่ข่าย)
2. **Ubuntu Server 22.04 ISO**
3. **Router TP-LINK Archer AX10** (Wireless AX1500)
4. **ESP32** + Sensor (DHT22 หรือ Sensor อื่น)
5. **VS Code** + Extension: Remote SSH
6. **Arduino IDE** (สำหรับเขียนโค้ด ESP32)
7. **Draw.io** (Offline)
8. **MQTT Explorer** (Desktop)
9. **Thunder Client** (VS Code Extension)
10. **Node.js Template** (Express + JWT)
11. **Vue 3 Template** (Nuxt 3 + Tailwind)

---

## 🚨 สิ่งที่ต้องระวัง

1. **เวลา** - อย่าใช้เวลามากกับส่วนที่ไม่สำคัญ
2. **Router Setup** - ตั้งค่า Router TP-LINK ให้ถูกต้อง (SSID: @iot, Password: 12345678)
3. **ESP32 WiFi** - ตรวจสอบให้ ESP32 เชื่อมต่อ WiFi ได้ก่อน
4. **MQTT Broker IP** - ใช้ IP ของ Production VM (192.168.1.20) ในโค้ด ESP32
5. **VS Code Remote** - ตั้งค่า SSH key ให้ถูกต้อง
6. **Path** - เก็บไฟล์ทั้งหมดใน `/home/it/project/`
7. **UI** - ไม่ต้องสวยมาก แค่ใช้งานได้ + Responsive
8. **Documentation** - ต้องมี Flow Diagram, ER Diagram, API Doc
9. **Pipeline** - ต้องทำงานได้จริง (Auto trigger)

---

## ✅ สรุป: ทำอะไรบ้าง

### ต้องทำ (ได้คะแนน)
1. ✅ Network Setup (20 คะแนน) - Proxmox + Production VM
2. ✅ Router Setup - TP-LINK Archer AX10 (SSID: @iot)
3. ✅ VS Code Remote SSH - เชื่อมต่อไปที่ `/home/it/project/`
4. ✅ MQTT + Node-RED (10 คะแนน) - ใช้ ESP32 จริง
5. ✅ ESP32 Hardware Integration - Pub/Sub กับ MQTT
6. ✅ Flow Diagrams (5 คะแนน)
7. ✅ Database Design (5 คะแนน)
8. ✅ Screen Design (5 คะแนน)
9. ✅ API Design (5 คะแนน)
10. ✅ Backend API (10 คะแนน)
11. ✅ Frontend (10 คะแนน)
12. ✅ Docker + CI/CD (รวมในส่วนอื่น)

### ไม่ต้องทำ (ประหยัดเวลา)
1. ❌ Mobile App
2. ❌ Complex Features
3. ❌ Beautiful UI (แค่ใช้งานได้)
4. ❌ GitLab VM แยก (ใช้ Docker หรือ GitLab.com)

**เป้าหมาย: 70-75 คะแนน จาก 80 คะแนน (87-94%)**

---

## 🔧 การตั้งค่า Router TP-LINK Archer AX10

### ข้อมูลการเชื่อมต่อ
- **Router Model:** TP-LINK Archer AX10 (Wireless AX1500 Dual Band Gigabit WiFi 6)
- **SSID:** `@iot`
- **Password:** `12345678`
- **IP Range:** 192.168.1.x (default)

### ขั้นตอนการตั้งค่า
1. เชื่อมต่อ Router กับ Internet
2. เข้า Web Interface: `http://192.168.1.1` (default)
3. ตั้งค่า Wireless:
   - SSID: `@iot`
   - Password: `12345678`
   - Security: WPA2/WPA3
4. ตั้งค่า Static IP สำหรับ Production VM: `192.168.1.20`

---

## 💻 การตั้งค่า VS Code Remote SSH

### ขั้นตอนการตั้งค่า
1. ติดตั้ง Extension: **Remote - SSH** ใน VS Code
2. สร้าง SSH Key (ถ้ายังไม่มี):
   ```bash
   ssh-keygen -t rsa -b 4096
   ```
3. Copy Public Key ไปที่ Production VM:
   ```bash
   ssh-copy-id it@192.168.1.20
   ```
4. ตั้งค่า VS Code:
   - กด `F1` หรือ `Ctrl+Shift+P`
   - พิมพ์: `Remote-SSH: Connect to Host`
   - พิมพ์: `it@192.168.1.20`
   - เปิด Folder: `/home/it/project/`

### ข้อดี
- แก้ไขโค้ดจากเครื่อง DEV ได้โดยตรง
- ไม่ต้อง copy ไฟล์ไปมา
- ใช้ Extension ของ VS Code ได้ปกติ

---

## 🔌 การตั้งค่า ESP32 + MQTT

### ข้อมูลการเชื่อมต่อ
- **WiFi SSID:** `@iot`
- **WiFi Password:** `12345678`
- **MQTT Broker:** `192.168.1.20` (IP ของ Production VM)
- **MQTT Port:** `1883` (default)

### Library ที่ต้องใช้ (Arduino IDE)
- `WiFi.h` (built-in)
- `PubSubClient.h` (ต้องติดตั้ง)
- `DHT.h` (ถ้าใช้ DHT22 sensor)

### โครงสร้างโค้ด ESP32
```cpp
// ESP32 MQTT Client
#include <WiFi.h>
#include <PubSubClient.h>

const char* ssid = "@iot";
const char* password = "12345678";
const char* mqtt_server = "192.168.1.20";

WiFiClient espClient;
PubSubClient client(espClient);

void setup() {
  // Connect WiFi
  // Connect MQTT
  // Setup Sensor
}

void loop() {
  // Read Sensor
  // Publish to MQTT
  // Subscribe from MQTT
}
```

